# AgenticAnalytics — Project Instructions

## Python Environment

- Always use `uv` as the package manager
- Activate the virtual environment before running any Python or pip commands:
  ```bash
  source .venv/Scripts/activate
  ```
- Run Python checks with: `source .venv/Scripts/activate && python ...`
     


## Planning

- Always start with plan.md file to get end-to-end project plan 
- after wrapping planning discussion, update plan.md file with the final plan
- udpate the #architecture.excalidraw file with a architecture diagram, output strictly valid excalidraw [using excalidraw skill] JSON only (no prose) representing a production-grade, layered, left-to-right system architecture with clearly labeled components and data flows.